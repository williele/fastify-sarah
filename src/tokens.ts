// access fastify instance
export const Fastify = Symbol("sarah:fastify");

// get current bootstrap options
export const BootstrapConfig = Symbol("sarah:bootstrap_options");

// use for access process schema function
export const ProcessSchema = Symbol("sarah:process_schema");
