// controller class instance
export const ControllerInst = Symbol("controller:instance");

// fastify instance
export const FastifyInst = Symbol("fastify:instance");

// bootstrap options (from register bootstrap)
export const BootstrapOpts = Symbol("bootstrap:options");
// bootstrap default configuration (root configuration)
export const BootstrapConfig = Symbol("bootstrap:config");
